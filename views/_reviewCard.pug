mixin reviewCard(review)
    .reviews__card
        .reviews__avatar
            img.reviews__avatar-img(src=`/img/users/${review.user.photo}`, alt=`${review.user.name}`)
            h6.reviews__user= review.user.name
        p.reviews__text= review.review
        .reviews__rating
            //- Use a loop and a ternary operator to put green or gray stars depending on the rating.
            //- For example, if review.rating is 3, in the first iteration star will be 1, so review.rating will be larger and hence
            //- we print a green star adding the reviews__star--active class. In the fourth operation star is larger so we print 'inactive'
            each star in [1, 2, 3, 4, 5]
                svg.reviews__star(class=`reviews__star--${review.rating >= star ? 'active' : 'inactive'}`)
                    use(href='/img/icons.svg#icon-star')